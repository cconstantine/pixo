syntax = "proto3";

package pixrpc;

message LocationStreamArgs {};

// The request message containing the user's name.
message Location {
  float x = 1;
  float y = 2;
  float z = 3;
}

service Tracking {
  rpc location_stream (LocationStreamArgs) returns (stream Location) {}
}
